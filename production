pipeline {
    agent {label 'srv2-zakaria'}
    stages {
        stage('Checkout SCM') {
            steps {
                git branch: 'production', url: 'https://github.com/zaxrmdn/simple-apps.git'
            }
        }
        stage('Deploy compose') {
            steps {
                sh '''
                docker compose build
                docker compose up -d
                '''
            }
        }
    }
}